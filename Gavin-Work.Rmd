#Gavin Work

```{r}
#Loading libraries
library(ggplot2)
library(reshape2)
library(readr)
library(dplyr)

```



```{r}
#Load the commodity dataset
HGP <- read.csv("Feed Grains Yearbook Tables-All Years.csv")

```

```{r}
str(HGP)

summary(HGP)

head(HGP)

HGP = HGP %>% rename('X' =)
```

```{r}
#plotting commodity prices over the years
ggplot(HGP, aes(x = year, y = ))
```







